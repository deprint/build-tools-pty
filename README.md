# Environment Module: ptyw.js

#### Spawn commands in pseudo-terminal.

This is a provider package for [build-tools](https://atom.io/packages/build-tools).

### Features
This module spawns a command in a pseudo-terminal (pty).
